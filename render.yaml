services:
  - type: web
    name: moodmuse-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js # or whatever your entry point is
    envVars:
      - key: NODE_VERSION
        value: 18
